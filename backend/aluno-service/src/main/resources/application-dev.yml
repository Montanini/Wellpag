# Ambiente de desenvolvimento local
# Valores fake, seguro versionar — nenhuma credencial real aqui.
# Mesmo banco (wellpag_dev) e mesmo segredo JWT do monolito (backend/application-dev.yml)
# nesta fase transitoria: o monolito continua escrevendo em "alunos" enquanto ainda
# esta em producao, e tokens emitidos pelo monolito continuam validos aqui.

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/wellpag_dev

server:
  port: 8093

wellpag:
  jwt:
    secret: dev-fake-secret-nao-usar-em-producao-min-256-bits-0000000000000000
  agenda-service:
    base-url: ${AGENDA_SERVICE_URL:http://localhost:8094}
  financeiro-service:
    base-url: ${FINANCEIRO_SERVICE_URL:http://localhost:8095}

logging:
  level:
    root: INFO
    com.wellpag.aluno: DEBUG
